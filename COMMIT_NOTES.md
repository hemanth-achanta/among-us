## Changes in this commit

- Configure Cursor automation rules to:
  - Generate a per-commit `COMMIT_NOTES.md` summary that overwrites previous contents.
  - Record a Y/N run status for each commit.
  - Avoid committing obvious secret files.

Run status: Y
